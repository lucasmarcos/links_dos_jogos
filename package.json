{
  "name": "links-dos-jogos",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun run gerar_html.ts",
    "lint": "biome check *.ts package.json tsconfig.json biome.json",
    "typecheck": "tsgo",
    "buildcss": "tailwindcss -i input.css -o output.css"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@biomejs/biome": "^2.5.6",
    "typescript": "^7.0.2",
    "@tailwindcss/cli": "^4.3.3"
  }
}
